import React from 'react';
const styles = require('../../styles/app.scss');

const Header = () => {
	return (
		<header className={styles.header}>
			<h1 className={styles.wrapper}>React Starter Kit</h1>
		</header>
	);
};

export default Header;
